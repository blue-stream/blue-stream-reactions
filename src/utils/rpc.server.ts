const jayson = require('jayson/promise');

export const RPCServer = new jayson.Server({
    async getReactionsByResources(resources: string[]) {
        // TODO: Call your function here and return desired output.
    },
});
